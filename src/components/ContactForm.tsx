"use client";
import {useState,type FormEvent} from "react";
import {ArrowUpRight} from "lucide-react";
export default function ContactForm(){
 const [opened,setOpened]=useState(false);
 const submit=(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();const d=new FormData(event.currentTarget);const body=`Name: ${d.get("firstName")} ${d.get("lastName")}\nEmail: ${d.get("email")}\nPhone: ${d.get("phone")||"Not provided"}\n\n${d.get("message")}`;window.location.href=`mailto:research@ip-6.net?subject=${encodeURIComponent(String(d.get("subject")))}&body=${encodeURIComponent(body)}`;setOpened(true)};
 return <div className="form-panel"><h2>Send a research inquiry</h2><p>Complete this form to prepare a draft in your email application. You will review and send it there. All fields are required except phone.</p><form onSubmit={submit}><div className="field-grid"><div className="field"><label htmlFor="firstName">First name</label><input id="firstName" name="firstName" autoComplete="given-name" maxLength={80} required/></div><div className="field"><label htmlFor="lastName">Last name</label><input id="lastName" name="lastName" autoComplete="family-name" maxLength={80} required/></div></div><div className="field"><label htmlFor="email">Email address</label><input id="email" name="email" type="email" autoComplete="email" maxLength={160} required/></div><div className="field"><label htmlFor="phone">Phone <span className="muted">(optional)</span></label><input id="phone" name="phone" type="tel" autoComplete="tel" maxLength={40}/></div><div className="field"><label htmlFor="subject">Subject</label><input id="subject" name="subject" maxLength={150} placeholder="Research or publication inquiry" required/></div><div className="field"><label htmlFor="message">Your message</label><textarea id="message" name="message" rows={6} maxLength={4000} required/></div><button type="submit" className="button">Prepare email <ArrowUpRight size={18}/></button><p className="form-notice" role="status">{opened?"Your email application was requested. Your message has not been sent by this website. If no draft appeared, email research@ip-6.net directly. Your entries remain here.":"Your message is sent only when you choose Send in your email application."}</p></form></div>
}